[0, "x01] & 0xF0 == 0x00` + writeable round-trip — NOT `== 0x04` rigidly | Audiobot hard lesson: revision nibble varies, upper nibble is the truth |\n| 4 | **Reg 0x04 = 0x36 for TDM** — 0x49 is Left-Justified and breaks TDM framing | Audiobot + Pt V2 both confirm |\n| 5 | **Clear PDN (reg 0x02) LAST** — after all other registers configured | Datasheet + practice |\n| 6 | **IO_GPIO_DIR_EN=false** in current NeoRV32 build → cannot drive codec RST from SW | External pull-up on Br saves us |\n| 7 | **IO_CLINT_EN=false** → no timer interrupts, no bootloader heartbeat LED | Doesn't break bootloader, just no blink |\n\n---\n\n## 🛠️ Firmware Macros (for C code)\n\n```c\n// TWI setup for CS42448 (100 kHz standard mode target)\n#define TWI_PRSC   6   // 160MHz / 2048 = 78.125 kHz\n#define TWI_CDIV  15   // /16 = 4.88 kHz (conservative)\n#define TWI_STRETCH 0\n\n// I²C addresses\n#define CS42448_ADDR  0x48\n#define SI5351A_ADDR  0x60\n\n// CS42448 registers\n#define CS42448_CHIP_ID     0x01\n#define CS42448_PWRCTL      0x02\n#define CS42448_IFACE1      0x03\n#define CS42448_DIF         0x04\n#define CS42448_ADC_CTRL    0x05\n#define CS42448_DAC_CTRL    0x06\n#define CS42448_MISC_CTRL   0x07\n\n// TDM values\n#define CS42448_DIF_TDM     0x36   // ADC_DIF=110, DAC_DIF=110\n#define CS42448_DIF_LJ      0x49   // Left-Justified (WRONG for TDM)\n#define CS42448_IFACE1_VAL  0xF0\n#define CS42448_ADC_CTRL_VAL 0x00\n#define CS42448_DAC_CTRL_VAL 0x00\n#define CS42448_MISC_CTRL_VAL 0xE4\n#define CS42448_PWRCTL_ON   0x00   // PDN clear\n\n// Inline helpers\nstatic inline void cs42448_init_twi(void) {\n    neorv32_twi_setup(TWI_PRSC, TWI_CDIV, TWI_STRETCH);\n}\n\nstatic inline uint8_t cs42448_read(uint8_t reg) {\n    return neorv32_twi_read(CS42448_ADDR, reg);\n}\n\nstatic inline void cs42448_write(uint8_t reg, uint8_t val) {\n    neorv32_twi_write(CS42448_ADDR, reg, val);\n}\n\nstatic inline bool cs42448_probe(void) {\n    uint8_t id = cs42448_read(CS42448_CHIP_ID);\n    return (id & 0xF0) == 0x00;  // Upper nibble = 0x0 = CS42448\n}\n\nstatic inline void cs42448_config_tdm(void) {\n    cs42448_write(CS42448_IFACE1, CS42448_IFACE1_VAL);\n    cs42448_write(CS42448_DIF,     CS42448_DIF_TDM);      // 0x36\n    cs42448_write(CS42448_ADC_CTRL, CS42448_ADC_CTRL_VAL);\n    cs42448_write(CS42448_DAC_CTRL, CS42448_DAC_CTRL_VAL);\n    cs42448_write(CS42448_MISC_CTRL, CS42448_MISC_CTRL_VAL);\n    cs42448_write(CS42448_PWRCTL,   CS42448_PWRCTL_ON);   // LAST\n}\n```\n\n---\n\n## 🔄 JTAG + UART Workflow (Daily Driver)\n\n```bash\n# Terminal 1: J-Link GDB Server (or SES)\nJLinkGDBServer -device RISC-V -if JTAG -speed 20000 -endian little -port 2331\n\n# Terminal 2: Load firmware via GDB\nriscv-none-elf-gdb build/debug_console.elf\n(gdb) target remote :2331\n(gdb) load\n(gdb) continue\n\n# Terminal 3: UART Console\npython3 scripts/rp2040_uart_bridge.py\n# → i2c scan\n# → i2c read8 0x48 0x04\n# → i2c write8 0x48 0x04 0x36\n```\n\n---\n\n## 📍 Pin Reference (LOCKED)\n\n| Signal | FPGA Pin | Function |\n|--------|----------|----------|\n| SCL | **B21** | I²C Clock |\n| SDA | **E21** | I²C Data |\n| MCLK | **D16** | 24.576 MHz to codec |\n| SCLK | **A19** | TDM Bit Clock |\n| LRCLK | **A18** | TDM Frame Sync |\n| DIN | **A21** | TDM Data In (ADC → FPGA) |\n| DOUT | **D21** | TDM Data Out (FPGA → DAC) |\n| GPIO[0] | **A15** | Codec control |\n| GPIO[1] | **A16** | Codec control |\n| GPIO[2] | **E16** | Codec control |\n| JTAG TCK | **AA3** | J-Link (independent!) |\n| JTAG TDI | **AB1** | J-Link |\n| JTAG TDO | **AA1** | J-Link |\n| JTAG TMS | **Y3** | J-Link |\n\n---\n\n## 🔗 Related Files\n- **Canonical pin map:** `~/.openclaw-godbot/workspace/projects/codec-ft601/PROJECT-STATUS-CANONICAL.md` §3\n- **TDM register proof:** `~/.openclaw-audio-companion/workspaces/audiobot/memory/` (Arty sessions)\n- **Critic knowledge:** 3 collections in WebUI (core, project, datasheets)\n- **debug_console firmware:** `~/.openclaw-godbot/workspace/development/runtime/console/console/console.elf`\n\n---\n\n*Generated 2026-06-17 from validated hardware session. Do not rediscover — use this.*"]