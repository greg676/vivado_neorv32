[0, "# SPI Bus Validation - Actual Pinout (from provided .xdc)\n\nValidate SPI communication using the **actual pin assignments** from the .xdc file used for current `basic_top` bitstream.\n\n## 📍 Actual Pinout (from .xdc)\n\n### SPI0 Core (fixed)\n| Signal | FPGA Pin |\n|--------|----------|\n| SPI0_CLK | **W4** |\n| SPI0_DAT_I | **V4** |\n| SPI0_DAT_O | **AA4** |\n\n### GPIO (used for SPI chip selects)\n| Signal | FPGA Pin | Likely Function |\n|--------|----------|-----------------|\n| GPIO[0] | **A15** | **SPI_CS0 → Display (ILI9341)** |\n| GPIO[1] | **A16** | **SPI_CS1 → Touch (XPT2046)** |\n| GPIO[2] | **E16** | **SPI_CS2 → SD Card** |\n| GPIO[3] | **D14** | **SPI_CS3 → Flash (W25Q64)** |\n| GPIO[4] | **U5** | (available) |\n| GPIO[5] | **T5** | (available) |\n| GPIO[6] | **R4** | (available) |\n| GPIO[7] | **T4** | (available) |\n\n### Other Relevant Pins\n| Signal | FPGA Pin | Notes |\n|--------|----------|-------|\n| UART0_RXD | **F19** | (from .xdc) |\n| UART0_TXD | **B15** | (from .xdc) |\n| I²C_SCL | **B20** | (from .xdc) |\n| I²C_SDA | **C14** | (from .xdc) |\n| MCLK | **D16** | (from .xdc) |\n| LRCLK | **G21** | (from .xdc) |\n| SCLK | **A19** | (from .xdc) |\n| DIN | **U6/V9/V8** | (Dout_0[2:0] from .xdc) |\n| DOUT | **?** | (likely Dout_0) |\n\n## 🔧 Assumptions\n1. SPI controller is enabled in NeoRV32\n2. Chip selects are active-low GPIO outputs\n3. No SPI arbiter in this basic design (we manage CS via GPIO)\n4. Devices are connected as per silkscreen/expectation\n\n## 🧪 Validation Procedure\n\n### 1. Display (ILI9341) - CS0 (GPIO[0] = A15)\n**Expected**: Respond to register reads\n\n**Test**:\n1. Set GPIO[0] = 0 (assert CS0)\n2. Send SPI: 0x09 (RDDST - Read Display Status) + read 1 byte\n3. Set GPIO[0] = 1 (deassert CS0)\n\n**Success**: Returns non-0x00 value (status byte)\n\n### 2. Touch (XPT2046) - CS1 (GPIO[1] = A16)\n**Expected**: CHIP_ID register = 0x09\n\n**Test**:\n1. Set GPIO[1] = 0 (assert CS1)\n2. Send SPI: 0x00 (CHIP_ID read) + read 1 byte\n3. Set GPIO[1] = 1 (deassert CS1)\n\n**Success**: Returns 0x09\n\n### 3. SD Card - CS2 (GPIO[2] = E16)\n**Expected**: Respond to CMD0 with R1 = 0x01\n\n**Test**:\n1. Set GPIO[2] = 0 (assert CS2)\n2. Clock ≥10 cycles (send 0xFF x2)\n3. Send CMD0 (0x40) + arg=0 + CRC=0x95\n4. Read R1 response (should be 0x01)\n5. Set GPIO[2] = 1 (deassert CS2)\n\n**Success**: R1 = 0x01\n\n### 4. Flash (W25Q64) - CS3 (GPIO[3] = D14)\n**Expected**: RDID = 0xEF 0x40 0x17\n\n**Test**:\n1. Set GPIO[3] = 0 (assert CS3)\n2. Send SPI: 0x9F (RDID) + read 3 bytes\n3. Set GPIO[3] = 1 (deassert CS3)\n\n**Success**: Returns [0xEF, 0x40, 0x17]\n\n## 🛠️ Firmware Requirements\nBuild `spi_test` that:\n- Initializes NeoRV32 SPI (PRSC=3 for 20MHz max)\n- Provides GPIO control for CS lines\n- UART menu to select device and run test\n- Implements SPI send/receive functions\n\n```c\n// SPI setup (max 20MHz for NeoRV32 SPI)\neorv32_spi_setup(3, 0, 0, 0);  // PRSC=3, standard, CPOL=0, CPHA=0\n\n// GPIO setup\neorv32_gpio_cfg_input_en(0x00000000);\neorv32_gpio_cfg_output_en(0x000000FF);  // GPIO[0:7] as output\n\n// Assert CS_n (active low)\neorv32_gpio_port_clr(1UL << n);\n\n// Deassert CS_n\neorv32_gpio_port_set(1UL << n);\n\n// SPI transfer uint8_t\nstatic inline uint8_t spi_xchg(uint8_t tx) {\n    eorv32_spi_write(tx);\n    while (eorv32_spi_busy());\n    return eorv32_spi_read();\n}\n```\n\n## 📌 Success Criteria\n- At least one device responds correctly to its probe sequence\n- GPIO chip select mapping works as expected\n- No SPI bus lockups (can deassert and retry)\n\n## 📄 Next Steps\n1. Build `spi_test` firmware\n2. Load via J-Link GDB (or SES)\n3. Run tests via UART console\n4. Record results\n5. If SPI works: Proceed to firmware development\n6. If SPI fails: Check wiring/soldering with this pinout as reference\n\n*Use the actual .xdc pinout to validate what is physically wired.*"]